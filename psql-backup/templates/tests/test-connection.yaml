apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "psql-backup.fullname" . }}-test-connection"
  labels:
    {{- include "psql-backup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "psql-backup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
